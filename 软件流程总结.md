# 车载智能仪表盘系统 —— 软件流程总结

> 工程目录：`F:\theCarUser\smartCar`
> 硬件平台：STM32F407ZGT6（主控）+ STM32F103（×2 从机）+ ESP8266 + W25Q128 + ILI9341
> 本文档对四套工程代码的软件流程进行梳理：**Boot（F407 引导程序）**、**APP（F407 主控应用）**、**F1_1（F103-A 从机）**、**F1_2（F103-B 从机）**。

---

## 一、系统总体架构

### 1.1 硬件组成与角色分工

本系统是一个基于 CAN 总线的车载智能仪表盘，由 3 块 MCU 组成整车局域网，并通过 ESP8266 接入 OneNET 云平台，实现仪表显示、数据采集与远程 OTA 升级。

| 工程目录 | MCU | 角色 | 主要功能 |
| --- | --- | --- | --- |
| `Boot/` | STM32F407ZGT6 | 引导程序 BootLoader | OTA 固件搬运、固件校验、失败回滚、跳转 APP |
| `APP/` | STM32F407ZGT6 | 主控应用 | LVGL 仪表盘 UI、CAN 接收、ESP8266 联网（MQTT/OTA） |
| `F1_1/` | STM32F103（A 节点） | CAN 从机 A | 采集车速/转速/油量/水温，发送 CAN 帧 `0x200` |
| `F1_2/` | STM32F103（B 节点） | CAN 从机 B | 4 按键扫描，切换图标状态，发送 CAN 帧 `0x201` |

### 1.2 通信链路

```
                    ┌───────────── OneNET 云平台 ─────────────┐
                    │  MQTT 服务 (mqtts.heclouds.com:1883)     │
                    │  OTA HTTP 服务 (iot-api.heclouds.com:80) │
                    └───────────────┬─────────────────────────┘
                                    │ WiFi (AT 指令)
                            ┌───────┴────────┐
                            │   ESP8266 模块  │
                            └───────┬────────┘
                                    │ UART6 (DMA 空闲中断)
                    ┌───────────────┴────────────────────────┐
                    │         STM32F407 主控 (Boot + APP)      │
                    │  SPI1 ─ W25Q128 (16MB 外部 Flash)        │
                    │  SPI3/并口 ─ ILI9341 LCD + 触摸屏         │
                    │  I2C ─ AT24C02 (触摸校准参数)             │
                    │  IWDG ─ 看门狗 (复位/重启机制)           │
                    └───────────────┬────────────────────────┘
                                    │ CAN (500 kbps)
              ┌─────────────────────┼─────────────────────┐
        ┌─────┴─────┐         ┌─────┴─────┐
        │ F103-A     │         │ F103-B     │
        │ (F1_1)     │         │ (F1_2)     │
        │ USART1 数据 │         │ 4 按键输入  │
        │ → 0x200    │         │ → 0x201    │
        └───────────┘         └───────────┘
```

### 1.3 软件分层

所有工程均基于 **STM32CubeMX + HAL 库 + FreeRTOS（CMSIS-RTOS v2/v1）** 框架，采用统一的三层结构：

```
Core/       — HAL 初始化代码（main.c、外设 MX_xxx_Init、中断、freertos.c）
BSP/        — 板级驱动（W25Q128、ILI9341、触摸、ESP8266、EEPROM、LED、延时、I2C）
APP/ MID/   — 应用/中间层业务逻辑（CAN 解析、LVGL GUI、MQTT、OTA、标志区管理）
```

---

## 二、Boot 引导程序软件流程（STM32F407）

### 2.1 工程结构

```
Boot/
├── Core/Src/main.c        主入口，外设初始化后调用 BootLoader_Run()
├── Core/Src/{spi,usart,iwdg,dma,gpio}.c   SPI1/USART2/IWDG 等外设
├── BSP/w25q128.c          W25Q128 SPI Flash 驱动
└── APP/bootloader.c/h     BootLoader 核心逻辑
```

### 2.2 片上 Flash 扇区布局

| 扇区 | 大小 | 地址范围 | 用途 |
| --- | --- | --- | --- |
| Sector 0~3 | 16 KB ×4 | `0x08000000` ~ `0x0800FFFF` | **BootLoader 区**（共 64 KB） |
| Sector 4 | 64 KB | `0x08010000` ~ `0x0801FFFF` | **APP 起始** |
| Sector 5~11 | 128 KB ×7 | `0x08020000` ~ `0x080FFFFF` | APP 其余部分 |

> APP 固件起始地址 `FLASH_APP_ADDR = 0x08010000`；APP 的 `main()` 中设置 `SCB->VTOR = 0x08010000` 以重定位向量表。

### 2.3 W25Q128 分区布局（16 MB）

| 分区 | 地址范围 | 大小 | 用途 |
| --- | --- | --- | --- |
| Zone1 下载区 | `0x000000` ~ `0x5FFFFF` | 6 MB | OTA 下载的新固件存放处 |
| Zone2 备份区 | `0x600000` ~ `0xBFFFFF` | 6 MB | 上一个成功固件备份（回滚源） |
| Zone3 标志区 | `0xC00000` ~ `0xC0FFFF` | 64 KB | OTA 标志/版本/进度等元数据 |

### 2.4 主流程（`BootLoader_Run()`）

`BootLoader_Run()` 是阻塞函数，上电后执行以下步骤：

1. **初始化 W25Q128**，通过串口打印启动横幅。
2. **读取并打印整个标志区**（upgrade_flag、protect_flag、各版本号、fw_size、boot_fail_cnt、downloaded_size、file_md5）。
3. **按 `upgrade_flag` 分流决策**：

```
BootLoader_Run()
  ├─ 决策 A：upgrade_flag == COMMITTED (0x55AA55AA)
  │    ├─ protect_flag == OK (0xBB66BB66)
  │    │    → APP 已确认新固件正常 → 清除所有标志 → 跳转 APP
  │    └─ protect_flag != OK
  │         → boot_fail_cnt++，写入标志区
  │         → 若 boot_fail_cnt >= 3：
  │              ├─ 备份区有效 → DoRollback() 回滚 → 看门狗复位
  │              └─ 无有效备份 → 清标志继续跳转
  │         → 跳转 APP
  │
  ├─ 决策 B：upgrade_flag != READY (0xAA55AA55)
  │    → 无待升级固件 → 直接跳转 APP
  │
  └─ upgrade_flag == READY：执行固件升级
       ① 验证固件头（SP 指向 SRAM 0x20000000~0x20030000）
       ② 擦除 APP Flash 区（按扇区，喂狗防复位）
       ③ 搬运 Zone1 → APP Flash（按字编程，喂狗）
       ④ 回读前 1KB 校验
       ⑤ 设 upgrade_flag=COMMITTED、清 protect/boot_fail_cnt、写 current_ver
       ⑥ 缩短 IWDG 超时 + 停止喂狗 → 看门狗复位
```

### 2.5 关键子流程

- **`Jump_to_APP(addr)`**：校验 APP 头部 SP 合法后，DeInit 外设、关 SysTick、设 MSP/PC、跳转（不返回）。
- **`EraseAppArea(addr, size)`**：`GetSector()`/`GetSectorSize()` 定位扇区，逐个扇区擦除，每擦一扇区喂一次 IWDG。
- **`DoRollback()`**：从 Zone2 读旧固件 → 擦 APP 区 → 搬运写回 → 更新标志区（current_ver=backup_ver）。
- **`SetCommittedFlag()` / `ClearUpgradeComplete()` / `WriteFlagsWord()`**：对标志区执行 **读-改-擦-写（R-M-E-W）** 操作。

---

## 三、APP 主控应用软件流程（STM32F407）

### 3.1 工程结构

```
APP/
├── Core/Src/main.c            主入口（设置 VTOR、启动 FreeRTOS）
├── Core/Src/freertos.c        FreeRTOS 任务/信号量创建
├── Core/Src/{can,spi,usart,tim,iwdg,dma,gpio}.c
├── BSP/                       esp8266、w25q128、ili9341、touch、24cxx、led、delay、myiic
├── MID/                       ESP_MQTT、ota_flags、ota_http、md5
├── MID/lvgl/                  LVGL 图形库 + lv_port_disp/indev 移植
└── APP/                       APP_can、APP_ota、APP_lv_gui、APP_icons_all、bootloader
```

### 3.2 启动流程（`main()`）

1. `__set_PRIMASK(0)` 开中断，`SCB->VTOR = 0x08010000` 重定位向量表。
2. `HAL_Init()` → `SystemClock_Config()`（168 MHz，HSE+PLL）。
3. 外设初始化：`GPIO → DMA → SPI1 → TIM1 → USART2 → SPI3 → IWDG → CAN1 → USART6`。
4. `osKernelInitialize()` → `MX_FREERTOS_Init()` → `osKernelStart()`。

### 3.3 FreeRTOS 任务结构

| 任务 | 优先级 | 栈 | 功能 |
| --- | --- | --- | --- |
| `Lv_page_show` | Normal | 1024×4 | 初始化 LCD/触摸/W25Q128/LED/LVGL，构建仪表盘 UI，释放 `LVGL_READY` 信号量 |
| `LVGLTask` | AboveNormal | 2048×4 | 获取 `LVGL_READY` 后循环调用 `lv_timer_handler()` |
| `espRx` | BelowNormal | 512×4 | 复位 ESP8266 → 连 WiFi → 连 OneNET MQTT → 释放 `OTA_READY` → 循环 `ESP_MQTT_MsgProcess()` |
| `OTA` | Low | 1024×4 | 执行完整 OTA 流程 + 周期检查新版本 + 响应下载按钮 |
| `IWDG` | Low | 256×4 | 每 2 s 喂狗（下载完成时停止喂狗触发复位） |
| `CANHandle` | Normal | 256×4 | 初始化 CAN 滤波器 → 循环 `APP_CAN_Run()` 轮询解析 |

> 两个信号量用于任务间同步：`LVGL_READY`（UI 就绪）、`OTA_READY`（网络就绪）。

### 3.4 CAN 接收流程

`CAN_Task` → `APP_CAN_Init()` 配置滤波器（ID-Mask，掩码全 0 接收所有帧）并启动 CAN1 → `APP_CAN_Run()` 轮询 FIFO：

```
接收 0x200（仪表数据帧，DLC=8）：
  speed = data[0..1]  rpm = data[2..3]  fuel = data[4]  temp = data[5]
接收 0x201（图标状态帧，DLC=1）：
  icons = data[0]  （bit0 安全带 / bit1 车辆 / bit2 警告 / bit3 制动）
```

解析结果写入共享结构体 `g_vehicle`，由 LVGL 定时器 `timer_cb` 每 100 ms 读取刷新 UI。

### 3.5 LVGL 仪表盘 UI

- `Lv_show` 任务完成底层初始化后调用 `lv_gui()` 构建界面：左表盘（转速）、右表盘（车速）、油量/水温/里程/时间日期标签、4 个图标（车辆/安全带/警告/制动）、WiFi 状态、OTA 版本/进度/下载按钮。
- 每 100 ms 定时器 `timer_cb` 将 `g_vehicle` 数据刷到对应控件；图标按位掩码切换颜色（默认青色/警告红或黄）。
- 下载按钮注册回调 `APP_OTA_RequestDownload()`（只置标志，实际下载在 OTA 任务执行，避免阻塞 UI）。

### 3.6 ESP8266 / MQTT 通信

- **底层驱动（BSP/esp8266.c）**：通过 UART6 DMA 空闲中断接收，`parse_ipd()` 解析 `+IPD,<len>:<data>` 格式到 `TCPdata[]`。
- **MQTT（MID/ESP_MQTT.c）**：通过 `AT+MQTT*` 系列指令连接 OneNET，流程为：鉴权配置 `AT+MQTTUSERCFG` → 连接 `AT+MQTTCONN` → 订阅双主题 `AT+MQTTSUB`；提供属性上报 `ESP_MQTT_PublishProperty()`。
- **常态**：`espRx` 任务维持 MQTT 长连接，循环处理下发消息。

### 3.7 OTA 升级流程

OTA 功能由 `OTA_Task` 统一调度，HTTP 通信由 `ota_http.c` 用 ESP8266 原始 TCP + 手动构造 HTTP/1.1 请求实现（三步：上报版本 → 查询任务 → 分片下载）。

**APP 侧主流程（`APP_OTA_Run()` + 周期循环）**：

```
等待 OTA_READY（网络就绪）
  → 接管 ESP8266（挂起 espRx 任务 → 退出透传 → 释放 MQTT → 关闭 TCP）
  → 版本同步（写 current_ver）+ 标志区自检（非法则格式化）
  → 按 upgrade_flag 状态机分流：
       COMMITTED → 上报新版本 → 备份 Zone1→Zone2 → 设 protect=OK → 清标志 → 恢复 MQTT
       READY     → 本地 MD5 校验（合法则等 BootLoader，非法则清标志）
       NONE      → 联网查询任务 → 有更新则 gui_set_ota_available(1)
  → 恢复 MQTT（重连 + 恢复 espRx）
```

**周期检查循环**（每 10 s，短轮询 500 ms 快速响应按钮）：
1. 若 `ota_download_requested` → 执行 `APP_OTA_StartDownload()`。
2. 若 `ota_busy` → 跳过周期检查。
3. 到点后：暂停 MQTT → 接管 ESP8266 → `OTA_CheckTask()` 查询 → 比较版本 → 更新 UI → 恢复 MQTT。

**下载子流程（`APP_OTA_StartDownload()` → `OTA_DownloadFirmware()`）**：
1. 前置检查任务信息有效性。
2. **断点续传判断**：`new_ver == target_ver && downloaded_size > 0 && < total` 则续传，否则擦除 Zone1 重新下载。
3. 记录版本/大小/MD5 到标志区。
4. 建立一次 TCP 连接（Keep-Alive 复用），循环发送带 `Range: bytes=offset-end` 的 GET 请求分片下载（每片 3584 B），每片写入 W25Q128 并保存断点，片失败最多重试（断开重连）3 次。
5. 下载完成后回读固件计算 **MD5** 与服务器值对比。
6. 校验通过 → 清断点、清 protect_flag、设 `upgrade_flag = READY`。
7. 停止喂狗 + 缩短 IWDG 超时 → 看门狗复位进入 BootLoader。

---

## 四、F1_1 从机软件流程（STM32F103-A）

### 4.1 工程结构

```
F1_1/
├── Core/Src/main.c         主入口（HAL 初始化 + FreeRTOS）
├── Core/Src/freertos.c     创建单个 CANTask
├── Core/Src/{can,usart,gpio}.c
└── APP/APP_can.c/h         CAN 采集/发送逻辑
```

### 4.2 功能与流程

F103-A 节点负责采集仪表数据，经 CAN 发送给 F407 主控：

1. `main()` 初始化 `GPIO → CAN → USART1`，启动 FreeRTOS。
2. `Can_Task` 调用 `APP_CAN_Init()`（启动 CAN，串口中断接收），然后每 100 ms 调用 `APP_CAN_Send()`。

```
USART1 逐字节中断接收 → 累积到 line_buf
  → 遇到 '\n' 解析 "speed,rpm,fuel,temp"（sscanf %d,%d,%d,%d）
  → 写入 g_vehicle
APP_CAN_Send()：打包 g_vehicle → CAN 0x200（DLC=8）
  data[0..1]=speed  data[2..3]=rpm  data[4]=fuel  data[5]=temp
```

> 代码内置 `CAN_TEST_FLAG` 回环自测模式：`CAN_MODE_SILENT_LOOPBACK` 自发自收，串口字节 → CAN → 回环 → 串口回传，用于隔离调试。正式联调时注释该宏。

---

## 五、F1_2 从机软件流程（STM32F103-B）

### 5.1 工程结构

```
F1_2/
├── Core/Src/main.c         主入口
├── Core/Src/freertos.c     创建单个 CANTASK
├── Core/Src/{can,usart,gpio}.c
└── APP/APP_can.c/h         4 按键扫描 + 图标状态发送
```

### 5.2 功能与流程

F103-B 节点通过 4 个按键切换仪表图标状态，经 CAN 发送给 F407：

1. `main()` 初始化外设，启动 FreeRTOS。
2. `Can_Task` 调用 `APP_CAN_Init()`（启动 CAN），之后每 1 ms 调用 `APP_CAN_KeyScan()` 扫描按键。

**按键状态机消抖**（4 键独立状态机，20 ms 消抖）：

```
按键：Key1=PA15(安全带)  Key2=PB4(车辆)  Key3=PB6(警告)  Key4=PB8(制动)

IDLE ──(检测到低电平)──> PRESS_DBNC ──(连续低 20ms)──> PRESSED(动作一次:翻转图标位+发帧)
                                                        │
                                        (检测到高电平)──┘
                                                        ▼
                                  RELEASE_DBNC ──(连续高 20ms)──> IDLE
```

- 每次确认按下调用 `APP_CAN_HandleKey(icon_mask)`：`g_icons ^= icon_mask` 翻转对应图标位，然后 `APP_CAN_SendStatus()` 发送 `0x201`（DLC=1，`data[0]=g_icons`）。
- 同样内置 `CAN_TEST_FLAG` 回环自测模式。

---

## 六、CAN 通信协议

整车 CAN 网络：2 × F103 + 1 × F407，波特率 500 kbps，标准帧。

| 报文 ID | 方向 | DLC | 数据含义 |
| --- | --- | --- | --- |
| `0x200` | F103-A → F407 | 8 | Byte0-1 车速（uint16，小端）；Byte2-3 转速（uint16，0~8000）；Byte4 油量（0~100 %）；Byte5 水温（0~120 ℃）；Byte6-7 保留 |
| `0x201` | F103-B → F407 | 1 | Byte0 图标位图：bit0 安全带 / bit1 车辆 / bit2 黄色警告 / bit3 制动 |

---

## 七、OTA 升级完整流程（含回滚）

### 7.1 升级状态机

```
NONE (0x00000000)
  │  APP 下载固件到 Zone1 + MD5 校验通过
  ▼
READY (0xAA55AA55)        ← BootLoader 应执行搬运
  │  BootLoader 搬运 Zone1 → APP Flash
  ▼
COMMITTED (0x55AA55AA)    ← 等待 APP 确认
  │  APP 确认运行正常（备份 + protect=OK）
  ▼
NONE（清标志，升级完成）
```

### 7.2 完整时序（以 V1.0.0 → V1.3.7 为例）

| 阶段 | 执行者 | 动作 |
| --- | --- | --- |
| ① 联网查询 | APP | HTTP 上报版本 → 查询任务 → 发现新版本 → UI 提示 |
| ② 下载 | APP | 分片下载固件到 Zone1（Keep-Alive + 断点续传）→ MD5 校验 → 设 `READY` |
| ③ 复位 | APP | 停止喂狗 + 缩短 IWDG → 看门狗复位 |
| ④ 搬运 | BootLoader | 读 `READY` → 验证头部 → 擦 APP 区 → Zone1→APP Flash → 校验 → 设 `COMMITTED` → 复位 |
| ⑤ 确认 | APP | 检测 `COMMITTED` → 上报版本 → 备份 Zone1→Zone2 → 设 `protect=OK` → 清标志 |
| ⑥ 回滚（异常） | BootLoader | `COMMITTED` 但 `protect≠OK` → `boot_fail_cnt>=3` → Zone2→APP Flash 回滚 |

### 7.3 可靠性设计要点

- **A/B 双分区**：Zone1 下载区 + Zone2 备份区，新固件先在外部 Flash 校验通过才刷入内部 Flash。
- **三级标志**：`upgrade_flag`（升级状态）+ `protect_flag`（APP 确认）+ `boot_fail_cnt`（失败计数），配合看门狗复位实现"刷入→确认→否则回滚"。
- **MD5 校验**：下载完成后回读整固件计算 MD5，与平台值比对。
- **断点续传**：每片写入后持久化 `downloaded_size`，重启后匹配版本即可续传。
- **R-M-E-W 标志写**：W25Q128 最小擦除单位 4 KB，采用"读整扇区→改字节→擦→写回"保证同扇区其他字段不被破坏。

---

## 八、关键数据流汇总

```
数据采集:  USART1 "speed,rpm,fuel,temp" → F103-A g_vehicle → CAN 0x200
按键输入:  4 按键状态机 → F103-B g_icons → CAN 0x201
仪表显示:  CAN 0x200/0x201 → F407 g_vehicle → LVGL timer_cb → 仪表盘刷新
云平台:    F407 → ESP8266(AT) → OneNET MQTT（属性上报/控制下发）
OTA:       OneNET OTA → ESP8266(HTTP) → W25Q128 Zone1 → BootLoader → 内部 Flash
```

---

## 附：四套工程关键文件对照

| 工程 | 核心文件 | 作用 |
| --- | --- | --- |
| Boot | `APP/bootloader.c` | 固件搬运/回滚/跳转主逻辑 |
| Boot | `BSP/w25q128.c` | 外部 Flash 驱动 |
| APP | `Core/Src/freertos.c` | 6 任务 + 2 信号量 |
| APP | `APP/APP_can.c` | CAN 接收解析 |
| APP | `APP/APP_lv_gui.c` | 仪表盘 UI 构建与刷新 |
| APP | `APP/APP_ota.c` | OTA 状态机调度 |
| APP | `MID/ota_http.c` | HTTP 上报/查询/分片下载 |
| APP | `MID/ota_flags.c` | 标志区 R-M-E-W 管理 |
| APP | `MID/ESP_MQTT.c` | MQTT 连接/订阅/上报 |
| F1_1 | `APP/APP_can.c` | 串口采集 → CAN 0x200 |
| F1_2 | `APP/APP_can.c` | 按键消抖 → CAN 0x201 |
